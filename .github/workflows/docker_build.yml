name: podman Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Login to AWS ECR
      run: |
        # aws ecr get-login-password --region us-east-1 | podman login --username AWS --password-stdin 351967858401.dkr.ecr.us-east-1.amazonaws.com
        podman build -t sdc_aws_lambda_promtail .
        podman tag sdc_aws_lambda_promtail:latest 351967858401.dkr.ecr.us-east-1.amazonaws.com/sdc_aws_lambda_promtail:latest
        podman push 351967858401.dkr.ecr.us-east-1.amazonaws.com/sdc_aws_lambda_promtail:latest
